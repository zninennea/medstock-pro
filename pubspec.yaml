name: medstock_pro
description: Advanced Pharmacy Inventory System
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.1.0
  
  flutter:
    sdk: flutter
  
  # Firebase - Exact versions that work together
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  firebase_storage: ^12.3.0

  # State Management
  provider: ^6.1.1
  shared_preferences: ^2.2.2

  # Utilities
  intl: ^0.19.0
  
  # UI
  fl_chart: ^0.68.0
  excel: ^4.0.0
  share_plus: ^10.1.0
  path_provider: ^2.1.2
  file_picker: ^11.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
